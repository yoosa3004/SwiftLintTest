name: SwiftLint
on:
  pull_request:
    paths:
      - '.github/workflows/swift.yml'
jobs:
  echo:
    runs-on: macos-latest
    steps:
      - name: Run SwiftLint
        run: ${PODS_ROOT}/SwiftLint/swiftlint --config .swiflint.yml
