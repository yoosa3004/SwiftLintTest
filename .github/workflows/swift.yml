name: SwiftLint
on:
  pull_request:
    paths:
      - '.github/workflows/swift.yml'
jobs:
  echo:
    runs-on: macos-latest
    steps:
      - name: Run SwiftLint
        run: swiftlint --path SwiftLintTest/.swiftlint.yml
